{
  // Solution file: the whole-workspace graph for `tsc -b tsconfig.json`.
  // `files: []` keeps it program-less; each package owns its own project.
  "extends": "./tsconfig.base.json",
  "files": [],
  "references": [
    { "path": "./packages/balance" },
    { "path": "./packages/quote-message" }
  ]
}
